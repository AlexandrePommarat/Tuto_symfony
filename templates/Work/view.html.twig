
{% extends "layout.html.twig" %}

{% block title %}
    Description d'une oeuvre - {{ parent() }}
{% endblock %}
{% block categorie%}
    <h1>Oeuvre</h1>
{% endblock %}
{% block ocplatform_body %}

    <h2>{{ oeuvre.title }}</h2>

    <div class="well">


        {% for perso in listPerso %}
            <li>
                <a href="{{ path('app_character_details', {'id': perso.id}) }}">
                {{ perso.firstname }}  {{ perso.lastname }}
                </a>


                <a href="{{ path('app_character_delete', {'id': perso.id}) }}" class="btn btn-danger">
                    <i class="glyphicon glyphicon-trash"></i>
                </a>
            </li>

        {% endfor %}

        </p>
        <p>
            <a href="{{ path('app_home') }}" class="btn btn-default">
                <i class="glyphicon glyphicon-chevron-left"></i>
                Retour à la liste
            </a>
            <a href="{{ path('app_character_addByOeuvre', {'id': oeuvre.id}) }}" class="btn btn-default">
                <i class="glyphicon glyphicon-edit"></i>
                Ajouter un Personnage
            </a>
            <a href="{{ path('app_work_delete', {'id': oeuvre.id}) }}" class="btn btn-danger">
                <i class="glyphicon glyphicon-trash"></i>
                Supprimer l'oeuvre
            </a>
        </p>
    </div>

{% endblock %}